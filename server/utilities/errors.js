const noFilesFound="No files found";
const noValidType="No valid request type";
const noValidExtension="No Valid Extension file";
const noObjFindOnDB="No object found with that id";
const wrongUser = "(User) or password are not correct";
const wrongPassword = "User or (password) are not correct";
const alreadyAuth = "Must to authenticate in a normal way";
const noFilesFound="No files found";
const noFilesFound="No files found";
const noFilesFound="No files found";
const noFilesFound="No files found";

module.exports={
    noFilesFound,
    noValidType,
    noObjFindOnDB,
    noValidExtension,
    wrongPassword,
    wrongUser,
    alreadyAuth
}